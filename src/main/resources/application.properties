spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/asturhome}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:password}
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

jwt.secret=${JWT_SECRET:asturhome-super-secret-jwt-key-2024-must-be-long-enough-for-hs256}
jwt.expiration=86400000

server.port=${PORT:8080}

upload.dir=uploads
upload.base-url=${UPLOAD_BASE_URL:http://localhost:8080/uploads}
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:oscarss1591@gmail.com}
spring.mail.password=${MAIL_PASSWORD:lfudadlncxdlefas}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.frontend-url=${FRONTEND_URL:http://localhost:5173}
app.agente-email=${AGENTE_EMAIL:asturhome.agente@gmail.com}
